const std = @import("std");

/// Stage 1 stub. Stage 2 implements WH_MOUSE_LL input + SendInput output.
pub fn run(init: std.process.Init) !void {
    _ = init;
    std.debug.print("zclicker: backend Windows ainda em construção (stub Stage 1).\n", .{});
}
